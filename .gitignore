soljson-*.js
secrets.mjs
.nginx/domains/*
.nginx/nginx.conf
.certs/*
.http01-challenge/*
.logs/*
.idea
scratch.html
node_modules
jspm_packages
test_modules
.npm
data.json
package-lock\.json
.npmrc
.env
.test.env
test/config/
*.snap
*.heapsnapshot
.vscode/
server.cert
server.key
dist/
cache/
# Solidity compiler output. The shipped contract ABIs/bytecode still reach npm
# via .npmignore (which does NOT ignore artifacts/), so ignoring them here only
# keeps the git tree clean of compiler output — it does not affect the published
# package.
artifacts/
